# Restart V13.28 修改紀錄

## 記帳收據照片

- 修正開啟記帳視窗時沒有清空檔案選擇欄位的問題。舊版存檔後 `<input type="file">` 仍握著上一張照片，下一筆記帳會被重複帶入同一張收據。
- 開啟記帳視窗時一併清除照片預覽、OCR 狀態與「重新辨識」按鈕。
- 新增「移除照片」按鈕：編輯已有照片的記帳時出現，按下後儲存即清除，雲端 Storage 上的檔案也會一併刪除。
- 換照片時會自動刪除舊的雲端檔案，不再留下孤兒檔案。

## 版本

- 專案版本更新為 V13.28，CSS／JS／Service Worker 快取版本同步更新。
- 本次沒有修改資料庫，因此不附 SQL。
