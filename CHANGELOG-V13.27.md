# Restart V13.27 修改紀錄

## 安全性

- **廣場 XSS**：其他玩家傳來的裝備 id 一律比對商城清單，不在清單內就視為未裝備；角色 canvas 的所有屬性值改為跳脫輸出（`square-v13.js`、`avatar-v123.js`）。
- **戒賭幣**：每日任務與七日簽到的獎勵金額改由資料庫推算，不再相信前端傳來的數字；簽到獎勵識別碼限定 `signin-<數字>` 格式，且每天只能領一次（`restart-sql-v13-27.sql`）。

## 邏輯修正

- **每日任務日期**：資料庫時區為 UTC，台灣凌晨 0～8 點會被算成前一天，改用 `Asia/Taipei` 日期。
- **Excel 匯入撞號**：`uid()` 改用 UUID；記帳排序改為字串比較。
- **復賭記帳刪除**：不再顯示「財務繳款」說明，刪除時會把扣掉的帳戶餘額加回，復賭紀錄本身保留。
- **繳款記帳**：帳戶欄位補上 `asset:` 前綴，記帳列表可正常顯示扣款帳戶。
- **登入狀態重複套用**：`applySession` 會依帳號去重，token 更新不再重複查詢 profile 或覆蓋本機設定。

## 資料安全

- **帳號分離**：本機資料改為每個帳號一個 key，同一支手機換帳號不會看到別人的資料；首次登入會把未歸屬的舊資料移轉一次。
- **雲端備援**：新增 `app_snapshots` 整份快照同步，換手機或 Safari 清除網站資料後都能還原；雙邊都有變更時會先備份較舊的版本再套用。
- **圖片改存 Storage**：封面、SOS 照片與收據登入後上傳私有 bucket，本機只留路徑；舊的 base64 圖片會在登入後自動搬移。
- **儲存空間保護**：寫入失敗不再假裝成功，會明確提示使用者。
- **離線可用**：Supabase 程式庫載入失敗時改掛離線替身，並提供「先離線使用」入口；Service Worker 一併快取外部程式庫。

## 版本

- 專案版本更新為 V13.27，CSS／JS／Service Worker 快取版本同步更新。
- 新增檔案：`js/core-v14.js`、`restart-sql-v13-27.sql`、本檔。
- 請執行 `restart-sql-v13-27.sql`。
