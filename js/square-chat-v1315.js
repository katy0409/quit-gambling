(function(){'use strict';const $=id=>document.getElementById(id);let channel=null,lastSent=0,user=null;const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));function render(rows){const box=$('squareChatMessages');if(!box)return;box.innerHTML=(rows||[]).slice().reverse().map(x=>`<article class="square-chat-message"><div><strong>${esc(x.display_name||'玩家')}</strong><time>${new Date(x.created_at).toLocaleTimeString('zh-TW',{hour:'2-digit',minute:'2-digit'})}</time></div><p>${esc(x.message)}</p></article>`).join('')||'<div class="small muted">目前沒有訊息</div>';box.scrollTop=box.scrollHeight}async function load(){if(!window.cloud)return;const {data}=await cloud.from('square_messages').select('*').order('created_at',{ascending:false}).limit(50);render(data||[])}async function start(){if(!window.cloud)return;const r=await cloud.auth.getUser();user=r.data?.user;if(!user)return;await load();if(channel)return;channel=cloud.channel('square-chat-v1315').on('postgres_changes',{event:'INSERT',schema:'public',table:'square_messages'},()=>load()).on('postgres_changes',{event:'DELETE',schema:'public',table:'square_messages'},()=>load()).subscribe()}async function send(e){e.preventDefault();const input=$('squareChatInput'),msg=input?.value.trim();if(!msg)return;if(Date.now()-lastSent<3000)return notify('請稍候再發送');if(msg.length>200)return notify('訊息最多 200 字');lastSent=Date.now();const name=user?.user_metadata?.display_name||user?.email?.split('@')[0]||'玩家';const {error}=await cloud.from('square_messages').insert({user_id:user.id,display_name:name,message:msg});if(error)return notify('訊息送出失敗：'+error.message);input.value=''}document.addEventListener('DOMContentLoaded',()=>{$('squareChatForm')?.addEventListener('submit',send);$('squareModal')?.addEventListener('click',()=>start(),{once:true})});window.RestartSquareChat={start,load};})();